# Concern: names the alpha crate and its path dependency on beta, one half of the cycle | Non-concern: the rule that flags it (.annotated-tree.toml owns it) | IO: none
[package]
name = "alpha"
version = "0.0.0"

[dependencies]
beta = { path = "../beta" }
