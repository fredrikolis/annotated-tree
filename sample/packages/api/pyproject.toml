# Concern: declares the acme-api package and the packages it depends on | Non-concern: the HTTP surface itself (acme_api/ owns it) | IO: none
[project]
name = "acme-api"
version = "0.1.0"
dependencies = [
    "acme-core",
    "fastapi>=0.110",
    "uvicorn>=0.29",
    "httpx>=0.27",
]
