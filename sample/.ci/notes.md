<!-- Concern: the release checklist the deploy step follows | Non-concern: performing the deploy (deploy owns that) | IO: none -->

# CI notes

A fixture doc inside a dot-directory, so the tree has one to reveal.
